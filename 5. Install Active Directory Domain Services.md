Run this on a fresh Windows Server VM that you'd making it to be your DC. <br>
Make sure that fresh VM is renamed, and IP/DNS configured if it haven't.

Install Active Directory Domain Services <br>
Name your Domain.
```powershell
Install-WindowsFeature AD-Domain-Services -IncludeManagementTools
Install-ADDSForest `
    -DomainName "adlab.test" `
    -DomainNetbiosName "ADLAB"
```
<br>

Verify
```powershell
Get-ADDomain
Get-ADForest
Get-ADDomainController
Resolve-DnsName adlab.test
```
